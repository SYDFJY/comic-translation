# 漫画翻译器 — AI 操作行为规范

**MangaTranslator · AI Module Operational Behavior Specification**

> 南昌航空大学 · 软件工程 2025 级暑期实训 · 漫画翻译器项目  
> 版本 v1.0 · 2026 年 7 月 10 日  
> 定义 AI 模块在软件中的全部操作行为边界、质量标准和降级策略

---

## 目录

1. [规范概述与适用范围](#1-规范概述与适用范围)
2. [AI 调用总原则](#2-ai-调用总原则)
3. [OCR 模块行为规范](#3-ocr-模块行为规范)
4. [翻译模块行为规范](#4-翻译模块行为规范)
5. [错误处理与重试策略](#5-错误处理与重试策略)
6. [速率限制与并发控制](#6-速率限制与并发控制)
7. [数据安全与隐私规范](#7-数据安全与隐私规范)
8. [翻译质量保障规范](#8-翻译质量保障规范)
9. [用户交互行为边界](#9-用户交互行为边界)
10. [日志与透明度规范](#10-日志与透明度规范)
11. [降级与容灾方案](#11-降级与容灾方案)
12. [规范检查清单](#12-规范检查清单)

---

## 1. 规范概述与适用范围

### 1.1 定义

本规范定义了漫画翻译器软件中所有涉及 AI（百度 OCR API、百度翻译 API）模块的操作行为边界——即 AI **在何时、以何种方式、在什么条件下**可以执行什么操作，以及在异常情况下应如何响应和降级。它是开发、测试和验收的共同依据。

### 1.2 适用范围

| 模块 | 涉及行为 | 规范章节 |
|------|---------|---------|
| 百度 OCR | API 调用时机、参数配置、结果处理、错误恢复 | [03](#3-ocr-模块行为规范) |
| 百度翻译 | API 调用时机、Prompt 模板、结果校验、错误恢复 | [04](#4-翻译模块行为规范) |
| Pipeline 编排 | 步骤串联、上下文传递、级联中断、进度反馈 | [05](#5-错误处理与重试策略) |
| 并发调度 | 速率限制、排队策略、批量并发控制 | [06](#6-速率限制与并发控制) |
| 安全存储 | 密钥加密、图片数据保护、传输安全 | [07](#7-数据安全与隐私规范) |
| 质量保障 | 置信度门控、人工修正闭环、质检流程 | [08](#8-翻译质量保障规范) |
| UI 交互 | AI 决策提示、用户兜底、信息透明 | [09](#9-用户交互行为边界) |
| 日志审计 | 操作记录、用户可见信息、追溯链路 | [10](#10-日志与透明度规范) |

### 1.3 术语表

| 术语 | 含义 |
|------|------|
| AI 模块 | 软件中调用外部 AI API 的所有代码组件（OcrService、TranslateService 等） |
| 级联中断 | 管线中某个步骤失败导致后续步骤全部中止的行为 |
| 降级 | 主方案不可用时切换到替代方案以保证基本功能可用 |
| 置信度 | AI 输出结果的可信程度量化值（OCR 为 probability，翻译由质检门控判定） |
| 熔断 | 连续失败达到阈值后暂停调用 AI API，防止无效请求堆积 |
| 人工兜底 | AI 无法完成时，允许用户手动操作替代 |

---

## 2. AI 调用总原则

以下是所有 AI 模块必须遵守的 **6 条核心原则**：

### 原则 1 — 用户知情同意 🔴

所有 AI 操作必须在用户**主动触发**后执行。AI 不得在后台静默调用 API。

**具体要求：**
- 点击"翻译"按钮 → Pipeline 启动 → AI 调用
- 首次启动引导中必须告知用户"本软件将调用百度云 API 处理您的图片，请确认您已阅读百度服务条款"
- 用户必须勾选"我已了解"才能继续

### 原则 2 — 最小必要原则 🔴

只向 API 发送完成当前任务所需的**最少数据**。

**具体要求：**
- OCR 只发送图片 Base64（不发送用户设备信息、不发送额外元数据）
- 翻译只发送文本内容和语言参数（不发送坐标、图片数据）
- 禁止在 API 请求中附加任何不必要的字段

### 原则 3 — 结果可验证 🟢

所有 AI 输出必须可被用户**验证和修正**。

**具体要求：**
- OCR 结果在原图上可视化标注（蓝色半透明框）
- 翻译结果在文本面板中逐条展示，双击可修正
- AI **不可覆盖**用户的最终决定

### 原则 4 — 失败可恢复 🟡

任何 AI 操作失败不得导致软件崩溃或数据丢失。

**具体要求：**
- API 调用失败 → 自动重试（最多 3 次）→ 熔断 → 降级方案 → 人工兜底
- 每个环节都有明确的恢复路径
- 原图始终保留，翻译结果作为新图层叠加，不破坏原始数据

### 原则 5 — 操作透明可追溯 🟢

所有 AI 操作必须记录日志，用户可查看关键信息。

**具体要求：**
- 每次 API 调用记录时间、API 类型、成功/失败、耗时
- 日志面板对用户可见；支持导出日志文件
- 不允许"黑箱"操作

### 原则 6 — 速率自觉 🟡

AI 模块必须自觉遵守 API 速率限制，不得超限调用。

**具体要求：**
- QPS 不得超过百度 API 免费额度上限
- 批量翻译时自动排队，并发 ≤ 3
- 遇限流响应（HTTP 429）立即暂停并排队等待，不得暴力重试

---

## 3. OCR 模块行为规范

### 3.1 调用时机与触发条件

| 触发场景 | 触发方式 | 调用版本 | 前置条件 |
|---------|---------|---------|---------|
| 单张翻译 | 用户点击"翻译"按钮 | general_basic（默认） | 图片已加载；API Key 已配置；access_token 有效 |
| 批量翻译 | 用户点击"批量翻译" | general_basic | 有待翻译项；API Key 有效；并发未满 |
| 重翻译 | 右键菜单 → 重翻译 | 用户设置的当前版本 | 原图仍在画布中 |
| 高精度模式 | 设置 → OCR 版本 | accurate_basic | 需显式切换；消耗更多免费额度 |

> 🔴 **禁止行为：** OCR 不得在以下场景自动调用：图片加载时（仅在画布展示，不自动 OCR）；用户切换文件时；后台定时触发。必须等待用户主动点击翻译按钮。

### 3.2 请求参数规范

OCR 请求参数（必须严格遵守）：

| 参数 | 值 |
|------|-----|
| image | 原图 Base64 编码（不含 `data:image/png;base64,` 前缀） |
| language_type | JAP（日漫默认，设置中可选 ENG/KOR/CHN） |
| detect_direction | `true`（**必须**开启，漫画有竖排文字） |
| detect_language | `true`（**必须**开启，自动检测源语言） |
| paragraph | `false`（漫画文字在气泡中，不需要段落合并） |
| probability | `true`（**必须**开启，用于置信度门控） |
| vertex_location | `true`（**必须**开启，需要四角坐标做区域覆盖） |

> 🟡 **禁止附加参数：** OCR 请求中不得附加任何百度 API 文档未规定的参数字段。不得传入自定义的 filter、mask、region 等字段。

### 3.3 结果处理流程

```
Step 1 → 解析 words_result 数组，提取 words、location、direction、probability
Step 2 → 置信度门控：probability < 0.5 的结果丢弃，不进入后续管线
Step 3 → 坐标扩展：将 location 向外扩展 expandMargin（默认12px），生成覆盖区域坐标
Step 4 → 重叠合并：两个区域重叠面积 ≥ 40% 时合并为一个区域，文字拼接
Step 5 → 方向标记：direction=1 标记为竖排，后续渲染时逐字符竖排绘制
Step 6 → 生成 List<TextRegion> 传入 PipelineContext，标注渲染到原图缩略图
```

### 3.4 OCR 级联规则

| OCR 结果状态 | 对后续步骤的影响 | 用户提示 |
|-------------|----------------|---------|
| 🟢 正常（≥1 个有效区域） | 全部步骤继续执行 | 底部进度条正常推进 |
| 🟡 部分失败（0 个有效区域，API 成功） | 管线中止于 Step 2，跳过翻译/修补/回填 | Toast："未检测到文字区域，请检查图片是否包含文字" |
| 🔴 API 失败 | 进入重试流程 | Toast："OCR 服务暂不可用，正在重试…" |
| 🟣 熔断（连续 5 次失败） | 熔断器开启，暂停所有 OCR 调用 60 秒 | Toast："OCR 服务暂时不可用，60秒后自动恢复" |

### 3.5 OCR 降级方案

| 层级 | 方案 | 触发条件 | 效果 |
|------|------|---------|------|
| **L0** | 百度 OCR 通用版 | 正常可用 | 完整 OCR 能力 |
| **L1** | 百度 OCR 高精度版 | 通用版识别率不足（用户主动切换） | 识别率提升，额度消耗更多 |
| **L2** | 用户手动标注 | API 完全不可用（熔断持续 > 5 分钟） | 用户手动框选文字区域并输入原文 |

> 注：L2 降级为 P2 优先级，MVP 版本不实现。熔断时直接提示用户稍后重试。

---

## 4. 翻译模块行为规范

### 4.1 调用时机与触发条件

| 触发场景 | 触发方式 | 前置条件 |
|---------|---------|---------|
| 管线自动触发 | OCR Step 完成后自动进入翻译 Step | OCR 返回 ≥ 1 个有效 TextRegion |
| 修正后重翻译 | 用户修正原文后点击"重新翻译" | 修正对话框中原文已修改 |
| 切换目标语言 | 设置 → 目标语言变更 → 标记"待重译" | 语言设置已保存 |

> 🔴 **禁止行为：**
> - 翻译模块不得脱离管线独立调用
> - 不得对空文本调用翻译 API
> - 不得对置信度已丢弃的 OCR 结果调用翻译
> - 不得在用户未触发翻译的情况下后台静默调用

### 4.2 请求参数规范

| 参数 | 值 |
|------|-----|
| q | 待翻译文本（单条，不超过 6000 字符） |
| from | `auto`（自动检测，依赖 OCR detect_language 结果） |
| to | `zh`（设置中可选：zh/en/ko/jp） |
| appid | 用户配置的百度翻译 App ID |
| salt | 随机数（UUID 或 Random Long） |
| sign | `MD5(appid + q + salt + secret_key)`（百度标准签名算法） |
| term_ids | **不传**（不使用术语库，避免额外依赖） |

### 4.3 翻译前后处理策略

**翻译前 — 文本拼接策略：**

```java
// 单条气泡翻译：直接传 q=原文
// 多气泡上下文拼接：当同一页存在多个气泡时，
//   按阅读顺序（从右到左、从上到下）将所有原文
//   拼接为一段，用换行符分隔，整体翻译。
//   翻译结果按行拆分回填到各气泡。
//
// 拼接格式：
// q = "气泡1原文\n气泡2原文\n气泡3原文"
// 翻译后按 \n 拆分结果
```

**翻译后 — 后处理规则：**

```java
// 1. 去除百度翻译返回的前缀/后缀噪声字符
// 2. 如果原文是短对话（≤15字），译文不应超过原文字数的2倍
// 3. 如果原文含感叹号/问号，译文必须保留对应标点
// 4. 翻译结果为空字符串 → 标记为"翻译失败"，进入重试
// 5. 翻译结果含 HTML 标签 → 剥离标签，只保留纯文本
```

### 4.4 翻译结果校验规范

| 校验维度 | 规则 | 处理方式 |
|---------|------|---------|
| 空结果 | trans_result 为空数组或 dst 为空字符串 | 标记为翻译失败，重试1次后提示用户 |
| 长度异常 | 译文长度 > 原文长度 × 3 | 警告标记 🟡，用户可在文本面板中修正 |
| 编码异常 | 译文含 Unicode 乱码（如 `\u0000` 等） | 丢弃该翻译结果，重试1次 |
| 标点缺失 | 原文有句末标点但译文无 | 自动补齐对应中文标点（。！？） |
| HTML 残留 | 译文含 `<` `>` `&` 等标签字符 | 正则剥离所有 HTML/XML 标签 |

### 4.5 翻译级联规则

| 翻译结果状态 | 对后续步骤的影响 | 用户提示 |
|-------------|----------------|---------|
| 🟢 正常（所有气泡翻译成功） | 进入 Step 4（图片修补） | 进度条推进 |
| 🟡 部分失败（部分气泡翻译为空） | 成功部分继续修补+回填；失败区域保留原文 | Toast："部分文字翻译失败，已保留原文" |
| 🔴 全部失败 | 管线中止于 Step 3 | Toast："翻译服务暂不可用，请稍后重试" |
| 🟣 熔断（连续 5 次失败） | 翻译熔断器开启，暂停 60 秒 | Toast + 状态栏红色指示器 |

### 4.6 翻译降级方案

| 层级 | 方案 | 触发条件 | 效果 |
|------|------|---------|------|
| **L0** | 百度翻译 API 标准版 | 正常可用 | 完整翻译能力 |
| **L1** | 百度翻译 API 批量模式 | 多气泡同页翻译（提高效率） | 一次请求翻译整页所有文本 |
| **L2** | 用户手动输入译文 | API 完全不可用（熔断持续 > 5 分钟） | 用户在修正对话框中手动输入译文 |

---

## 5. 错误处理与重试策略

### 5.1 错误分级体系

| 等级 | 类型 | 示例 | 处理方式 |
|------|------|------|---------|
| 🔴 **E1** | 网络不可达 | Connection refused、DNS 解析失败、Socket timeout | 自动重试（最多3次，指数退避）→ 熔断 → 降级 |
| 🟡 **E2** | API 业务错误 | error_code: 18（QPS超限）、110（token 无效） | QPS超限→排队等待；鉴权失败→提示重新配置 |
| 🔵 **E3** | API 限流 | HTTP 429 或 error_code: 18 | 暂停请求，排队等待后重试 |
| 🟣 **E4** | 结果质量异常 | OCR 置信度 < 0.5、翻译为空、译文过长 | 丢弃该条结果，不重试；部分失败继续管线 |
| 🟢 **E5** | 本地处理错误 | 图片编解码失败、Graphics2D 渲染异常 | 记录日志，跳过当前区域，不中断整个管线 |

### 5.2 重试矩阵（指数退避）

| 重试次数 | 等待时间 | 适用错误 | 不可重试的错误 |
|---------|---------|---------|--------------|
| 第1次 | 2 秒 | E1（网络）、E3（限流） | E2 鉴权失败（需用户操作） |
| 第2次 | 4 秒 | E1（网络）、E3（限流） | E4（结果质量，丢弃不重试） |
| 第3次 | 8 秒 | E1（网络） | E3 第3次仍限流 → 熔断 |

> 重试期间 UI 不阻塞，进度条显示"正在重试 (2/3)…"。3次重试全部失败后进入熔断评估。

### 5.3 熔断器机制

| 参数 | 值 |
|------|-----|
| **触发条件** | 连续 5 次 API 调用失败（含重试后仍失败） |
| **熔断时长** | 60 秒（半开状态允许 1 次探测请求） |
| **半开探测** | 熔断 60 秒后允许发送 1 次请求，成功则恢复，失败则继续熔断 |
| **最大熔断次数** | 3 次（3次熔断后进入降级模式，不再自动恢复） |
| **用户可见状态** | 状态栏指示器：🟢正常 / 🟡重试中 / 🔴熔断 |

> 🔴 **熔断期间禁止行为：** 禁止发起任何新的 API 请求；禁止自动重试；禁止在后台循环探测。必须等待 60 秒半开窗口，或用户手动点击"重试"。

---

## 6. 速率限制与并发控制

### 6.1 百度 API 免费额度与 QPS 上限

| API | 免费额度 | QPS 上限 | 超额行为 |
|-----|---------|---------|---------|
| 百度 OCR 通用版 | 50,000 次/月 | 2 QPS | error_code: 18 |
| 百度 OCR 高精度版 | 500 次/月 ⚠️额度极小 | 2 QPS | error_code: 18 |
| 百度翻译标准版 | 50,000 字符/月（标准）/ 2,000,000 字符/月（高级） | 10 QPS | error_code: 18 |
| 百度 access_token | 每次有效 30 天 | — | error_code: 110 |

### 6.2 本地速率控制策略

| 参数 | 值 |
|------|-----|
| **OCR QPS 上限** | 1.5 QPS（低于百度2QPS上限，留0.5QPS缓冲） |
| **翻译 QPS 上限** | 8 QPS（低于百度10QPS上限，留2QPS缓冲） |
| **批量翻译并发数** | 3（最多3张图片同时处理） |
| **排队策略** | FIFO（先进先出） |
| **额度预警阈值** | 80%（使用量达80%时 Toast 提醒用户） |
| **额度耗尽行为** | 暂停所有翻译操作，Toast 提示用户升级百度账号 |

### 6.3 限速实现方式

```java
// 令牌桶限速器（TokenBucketRateLimiter）
public class TokenBucketRateLimiter {
    private final double maxQps;          // 最大 QPS
    private final Semaphore semaphore;    // 令牌信号量
    private final ScheduledExecutorService scheduler;

    // 每 1000ms/maxQps 毫秒释放一个令牌
    // OCR: 1.5 QPS → 每 667ms 释放1令牌
    // 翻译: 8 QPS → 每 125ms 释放1令牌

    public boolean tryAcquire() {
        return semaphore.tryAcquire(5, TimeUnit.SECONDS);
    }
}
```

> 🟡 **限流响应处理：** 收到百度 QPS 超限响应（error_code: 18）时：① 立即停止发送新请求 → ② 将当前请求放入排队队列 → ③ 等待 1 秒后重新尝试 acquire 令牌 → ④ 3 次排队失败后标记为限流失败，Toast 提示用户。
>
> **禁止行为：** 不得在限流后立即重试；不得绕过令牌桶直接发送请求。

---

## 7. 数据安全与隐私规范

### 7.1 API 密钥安全

| 维度 | 规范 |
|------|------|
| **存储位置** | `~/.manga-translator/config.json`（用户主目录下，**不在**项目目录） |
| **加密方式** | AES-128 加密（密钥由本地机器指纹派生） |
| **禁止硬编码** | API Key 不得出现在源代码、配置模板、日志输出中 |
| **首次引导** | 引导对话框中输入 Key → 立即加密存储 → 原文不保留 |
| **验证连通** | 存储后调用一次百度 API 验证 → 成功后删除明文缓存 |
| **传输安全** | 所有 API 请求必须使用 HTTPS |

> 🔴 **密钥禁止行为：**
> 1. 不得将 API Key 写入源代码（包括注释、测试代码）
> 2. 不得在日志中记录 API Key（包括 sign 计算过程中的 secret_key）
> 3. 不得将 config.json 包含在 Git 版本控制中（.gitignore 必须排除）
> 4. 不得通过网络传输 API Key 的明文（除了向百度 API 发送签名时的必要参数）
> 5. 不得在 UI 上显示完整 API Key（只显示前4位+掩码，如 `abcd********`）

### 7.2 图片数据安全

| 维度 | 规则 | 实现方式 |
|------|------|---------|
| 原图保护 | 翻译过程不修改原图文件 | 所有操作在内存的 BufferedImage 副本上进行；导出时生成新文件 |
| 内存释放 | 图片处理完成后及时释放内存 | 单张完成后 dispose；批量最多保留 5 张在内存 |
| 临时文件 | 不创建临时图片文件 | Base64 编码在内存中完成，不写入磁盘 |
| 数据不上传 | 仅向百度 API 发送必要数据 | 不发送用户名、设备信息、IP 地址等 |

### 7.3 翻译结果数据

| 维度 | 规范 |
|------|------|
| 本地缓存 | 翻译结果缓存在 `~/.manga-translator/cache/`（按图片 SHA256 哈希索引） |
| 缓存有效期 | 7 天（超过自动清理） |
| 缓存内容 | 仅存翻译文本+坐标，不存原图 |
| 缓存清理 | 启动时自动清理；设置中提供"清除缓存"按钮 |
| 禁止外部传输 | 翻译结果不得通过网络传输到任何第三方服务 |

---

## 8. 翻译质量保障规范

### 8.1 置信度门控

| 等级 | 阈值 | 行为 |
|------|------|------|
| 🔴 丢弃 | < 0.5 | OCR 结果被丢弃，不进入后续管线 |
| 🟡 低置信度 | 0.5 ~ 0.7 | 结果标记为 🟡，用户可查看和修正 |
| 🟢 高置信度 | ≥ 0.8 | 自动进入翻译，文本面板显示 🟢 色标 |

- 文本面板每行显示置信度色标 🟢/🟡/🔴
- 用户可修正任何置信度等级的 OCR 原文（修正后重新翻译）

### 8.2 翻译质检流程

```
Step 1 → 百度翻译 API 返回译文
Step 2 → 空结果检测 → 为空则标记失败
Step 3 → 长度校验 → 译文长度/原文长度 > 3 → 警告标记
Step 4 → 标点校验 → 原文有标点但译文无 → 自动补齐
Step 5 → HTML剥离 → 译文含标签 → 正则清理
Step 6 → 通过质检 → 进入 Step 4（图片修补）；未通过 → 标记原因，用户可见
```

### 8.3 人工修正闭环

| 修正类型 | 触发方式 | 修正后行为 |
|---------|---------|-----------|
| 修正 OCR 原文 | 文本面板中双击原文列 | 修改原文 → 自动重新翻译该条 → 重新渲染 |
| 修正翻译译文 | 文本面板中双击译文列 | 修改译文 → 直接重新渲染（不重新调用翻译 API） |
| 修正区域坐标 | 画布上拖拽标注框边缘 [P2] | 调整覆盖区域 → 重新修补+渲染 |
| 删除某条翻译 | 文本面板中右键 → 删除 | 该区域保留原文不覆盖，不修补 |
| 撤销修正 | Ctrl+Z | 恢复上一个修正状态（最多 20 步历史） |

> 🟢 **AI 不可覆盖用户修正：** 用户修正后的译文优先级高于 AI 翻译结果。重新翻译时，已修正的条目不会被 AI 结果覆盖，除非用户主动点击"重置为 AI 翻译"。

---

## 9. 用户交互行为边界

### 9.1 AI 决策权 vs 用户决策权

| 决策场景 | AI 决策 | 用户决策 | 说明 |
|---------|---------|---------|------|
| 是否开始翻译 | ❌ | ✅ | 必须用户点击"翻译"按钮 |
| OCR 版本选择 | ❌ | ✅ | 用户在设置中选择通用版/高精度版 |
| 源语言检测 | ✅ 建议 | ✅ 最终 | AI 自动检测，用户可强制指定 |
| 置信度阈值 | ✅ 默认 | ✅ 可调 | 默认 0.5，可调范围 0.3-0.9 |
| 翻译结果采纳 | ✅ 默认 | ✅ 最终 | 用户可修正或删除任何条目 |
| 修补策略选择 | ❌ | ✅ | 白色覆盖/取色填充 |
| 回填字体选择 | ❌ | ✅ | 字体和字号策略 |
| 批量翻译启动 | ❌ | ✅ | 必须用户点击"批量翻译" |
| 导出格式 | ❌ | ✅ | JPG/PNG 和导出路径 |
| 熔断恢复 | ✅ 自动 | ✅ 手动 | 60秒半开自动探测；用户也可手动"重试" |

### 9.2 Toast 提示文案标准

| 场景 | 级别 | 文案模板 | 持续时间 |
|------|------|---------|---------|
| 翻译开始 | ℹ️ 信息 | "正在翻译第 {n}/{total} 张…" | 持续到完成 |
| 翻译完成 | ✅ 成功 | "翻译完成！共识别 {count} 个文字区域" | 3 秒 |
| OCR 失败重试 | ⚠️ 警告 | "OCR 服务暂不可用，正在重试 ({attempt}/3)…" | 持续到重试结果 |
| OCR 熔断 | ❌ 错误 | "OCR 服务暂时不可用，将在 60 秒后自动恢复" | 5 秒 |
| 翻译部分失败 | ⚠️ 警告 | "部分文字翻译失败（{fail}/{total}），已保留原文" | 5 秒 |
| 翻译全部失败 | ❌ 错误 | "翻译服务暂不可用，请稍后重试" | 5 秒 |
| 额度预警 | ⚠️ 警告 | "本月 API 额度已使用 {percent}%，请注意" | 5 秒 |
| 额度耗尽 | ❌ 错误 | "本月 API 额度已用完，请升级百度账号或下月再试" | 持续显示 |
| API Key 无效 | ❌ 错误 | "API Key 配置无效，请在设置中重新配置" | 持续显示+跳转设置 |
| 人工修正保存 | ✅ 成功 | "修正已保存，正在重新渲染…" | 2 秒 |

> 🔴 **提示文案禁止行为：**
> 1. 不得使用模糊文案如"出错了"、"网络异常"——必须指明具体服务（OCR/翻译）和具体原因
> 2. 不得使用技术术语面向普通用户——如"HTTP 429"应改为"服务繁忙"
> 3. 不得在错误提示中包含 API Key、请求 URL、响应 JSON 等敏感信息
> 4. 错误提示必须附带可操作的下一步建议

### 9.3 进度反馈规范

| 场景 | 显示方式 |
|------|---------|
| **单张翻译进度** | 5 段式：OCR 20% → 清洗 10% → 翻译 30% → 修补 20% → 回填 20% |
| **批量翻译进度** | 双层进度条：外层总进度（已完成/总数），内层当前文件进度（5段式） |
| **重试中进度** | 进度条暂停 + 脉冲动画 + 文字"正在重试…" |
| **熔断中进度** | 进度条红色 + 文字"服务暂不可用，等待恢复…" |
| **进度不可逆** | 进度条不得回退（某步骤失败时进度停在当前位置） |

---

## 10. 日志与透明度规范

### 10.1 日志记录粒度

| 日志级别 | 记录内容 | 用户可见 | 存储位置 |
|---------|---------|---------|---------|
| 🟢 **INFO** | API 调用开始/成功、步骤完成、翻译条目数 | ✅ 日志面板可见 | 本地日志文件 |
| 🟡 **WARN** | 重试触发、置信度低、额度预警、部分翻译失败 | ✅ 日志面板可见 | 本地日志文件 |
| 🔴 **ERROR** | API 调用失败、熔断触发、全部翻译失败、鉴权失败 | ✅ 日志面板可见（红色标记） | 本地日志文件 |
| 🟣 **DEBUG** | 请求参数详情、响应 JSON、坐标计算过程 | ❌ 默认不可见（设置中开启"调试模式"后可见） | 本地日志文件 |

### 10.2 日志脱敏规则

> 🔴 **日志中禁止记录的信息：**
> 1. API Key（全部掩码，如 `********`）
> 2. Secret Key（全部掩码）
> 3. access_token（前8位+掩码，如 `24.a8b3****`）
> 4. 图片 Base64 数据（仅记录长度，如 `image_base64_length=182340`）
> 5. 用户本地文件路径中的用户名部分（如 `C:/Users/****/Pictures/...`）
> 6. sign 计算过程中的中间值

### 10.3 日志格式标准

```
// 日志格式：ISO8601时间 | 级别 | 模块 | 消息 | 详情
2026-07-10T15:32:18.456 | INFO  | OCR    | 识别完成 | words=12, avg_prob=0.89
2026-07-10T15:32:19.123 | INFO  | TRANS  | 翻译完成 | items=12, src=jp, dst=zh
2026-07-10T15:32:20.001 | WARN  | OCR    | 低置信度 | region_id=5, prob=0.52
2026-07-10T15:32:21.500 | ERROR | TRANS  | API失败  | error_code=18, desc=QPS超限, retry=1/3
2026-07-10T15:33:01.200 | ERROR | OCR    | 熔断开启 | consecutive_failures=5, cooldown=60s
```

### 10.4 日志面板交互规范

| 交互 | 行为 | 参数 |
|------|------|------|
| 展开/折叠 | 点击状态栏"日志"按钮 → 日志面板从底部展开 | 高度 200px；展开动画 200ms ease-out |
| 筛选 | 下拉选择日志级别：ALL / INFO / WARN / ERROR | 默认 ALL；筛选即时生效 |
| 搜索 | 输入关键词搜索日志内容 | 匹配行高亮显示 |
| 导出 | 点击"导出日志"按钮 → 保存为 .log 文件 | 文件名：`manga-translator-log-{date}.log` |
| 清空 | 点击"清空日志"按钮 → 清空当前会话日志 | 需二次确认；清空仅清显示，不清文件 |

---

## 11. 降级与容灾方案

### 11.1 三级降级体系

| 级别 | 状态 | OCR | 翻译 | 用户操作 |
|------|------|-----|------|---------|
| 🟢 **L0** | 完全可用 | 百度 OCR 正常 | 百度翻译正常 | 一键翻译，全自动 |
| 🟡 **L1** | 部分降级 | 百度 OCR 可用 / 高精度版 | 百度翻译排队等待 | 翻译速度降低，功能完整 |
| 🔴 **L2** | 深度降级 | 百度 OCR 不可用（熔断） | 百度翻译不可用（熔断） | 需手动标注+手动输入译文 |

### 11.2 L0 → L1 降级触发与恢复

```
触发：QPS 限流（error_code: 18）
  ↓ 进入排队等待，UI 显示"服务繁忙，正在排队…"
等待：指数退避 2s → 4s
  ↓ 最多排队 2 次，成功则恢复 L0
恢复：排队成功获取令牌
  ↓ 恢复正常翻译速度，Toast "服务已恢复"
```

### 11.3 L1 → L2 降级触发与恢复

```
触发：连续 5 次失败 → 熔断开启
  ↓ 暂停所有 API 调用，UI 显示红色状态指示器
等待：熔断 60 秒
  ↓ 半开窗口允许 1 次探测请求
探测成功 → 恢复 L1（排队等待），逐渐恢复 L0
探测失败 → 继续熔断
  ↓ 3 次熔断后锁定 L2，不再自动恢复
L2 锁定后恢复：用户手动点击"重试" → 探测请求 → 成功则恢复 L0
```

### 11.4 离线兜底方案

完全离线场景（API 不可用 > 30 分钟）：

| 功能 | 离线替代方案 | 优先级 |
|------|------------|--------|
| OCR | 用户在画布上手动框选文字区域 + 手动输入原文 | P2 |
| 翻译 | 用户在修正对话框中手动输入译文 | P2 |
| 修补 | 白色覆盖（本地算法，无需 API） | P0 ✅ |
| 回填 | Graphics2D 渲染（本地算法，无需 API） | P0 ✅ |
| 导出 | ImageIO 写文件（本地算法，无需 API） | P0 ✅ |

> MVP 版本不实现 L2 离线 OCR/翻译替代方案。熔断时直接提示用户稍后重试或检查网络。

---

## 12. 规范检查清单

以下 **22 条**为开发、测试和验收的必检项目。每条必须通过才能视为符合规范：

| 编号 | 检查项 | 状态 |
|------|--------|------|
| 01 | 所有 AI 调用仅由用户主动触发，无后台静默调用 | ☐ |
| 02 | 首次启动引导包含"我已了解百度服务条款"确认勾选 | ☐ |
| 03 | API 请求中不含任何非必要参数字段 | ☐ |
| 04 | OCR 置信度门控生效，< 0.5 的结果被丢弃 | ☐ |
| 05 | 翻译空结果检测生效，空结果标记为失败 | ☐ |
| 06 | 翻译长度异常检测生效，超3倍长度标记警告 | ☐ |
| 07 | 翻译标点缺失自动补齐生效 | ☐ |
| 08 | HTML 标签剥离生效 | ☐ |
| 09 | 重试策略：最多3次，间隔 2/4/8s，指数退避 | ☐ |
| 10 | 熔断器：连续5次失败触发，60秒半开窗口 | ☐ |
| 11 | 熔断期间无新 API 请求发出 | ☐ |
| 12 | OCR 令牌桶限速 ≤ 1.5 QPS | ☐ |
| 13 | 翻译令牌桶限速 ≤ 8 QPS | ☐ |
| 14 | 批量翻译并发 ≤ 3 | ☐ |
| 15 | API Key AES-128 加密存储，源码中无硬编码 | ☐ |
| 16 | config.json 在 .gitignore 中排除 | ☐ |
| 17 | 原图文件不被翻译过程修改 | ☐ |
| 18 | 用户修正优先级高于 AI 结果，修正不被自动覆盖 | ☐ |
| 19 | 日志脱敏：API Key 全掩码，图片数据仅记长度 | ☐ |
| 20 | Toast 文案指明具体服务（OCR/翻译）和原因 | ☐ |
| 21 | 额度使用达 80% 时 Toast 预警提醒 | ☐ |
| 22 | 降级流程完整：L0 → L1 → L2 每级有明确触发和恢复路径 | ☐ |

---

## 版本信息

| 项目 | 内容 |
|------|------|
| 规范版本 | v1.0 |
| 制定日期 | 2026-07-10 |
| 适用项目 | 漫画翻译器（南昌航空大学 暑期实训） |
| 下次复审 | Phase 1 开发完成后（约 2 周后） |
| 维护者 | 项目开发团队 |

---

> 漫画翻译器 — AI 操作行为规范 v1.0 · 南昌航空大学软件工程 2025 级暑期实训
